{
  "dependencies": {
    "elm": "latest-0.19.1",
    "elm-format": "^0.8.2",
    "elm-review": "^2.7.1",
    "elm-test": "^0.19.0-rev6",
    "elm-verify-examples": "^5.0.0"
  },
  "scripts": {
    "test": "elm make && elm-format --validate src && elm-verify-examples  && elm-test && elm-review"
  }
}
